%% testFindLetters

I1 = imread('../01_list.jpg');
I2 = imread('../02_letters.jpg');
I3 = imread('../03_haiku.jpg');
I4 = imread('../04_deep.jpg');

I = I3;



[lines, bw] = findLetters(I);


% b = [lines{1}(i,1) lines{1}(i,2) lines{1}(i,3)-lines{1}(i,1) lines{1}(i,4)-lines{1}(i,2)]

% I1(lines.PixelIdxList{1,:}) = 0;
i = 3;
hold off
figure
hold on

imshow(I);

for i = 1:size(lines{1},1)
    b = [lines{1}(i,1) lines{1}(i,2) lines{1}(i,3)-lines{1}(i,1) lines{1}(i,4)-lines{1}(i,2)]
    rectangle('Position',b,'EdgeColor','b')
end

% figure
% imshow(I2);
% figure
% imshow(I3);
% figure
% imshow(I4);


%%
% [lines, bw] = findLetters(I1)